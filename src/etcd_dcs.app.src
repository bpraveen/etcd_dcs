{application, etcd_dcs, [
   {description, "etcd distributed co-ordindation service"},
   {vsn, hg},
   {registered, [etcd_dcs_sup, lock_sup, lock_mgr]},
   {applications, [kernel, stdlib, grpc_client, grpc_lib, cow_lib, http2_client, gpb]},
   {mod, {etcd_dcs,[]}},
   {env, []}
]}.
